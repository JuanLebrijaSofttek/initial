import Foundation

/// Legacy placeholder file retained to preserve the original project structure.
/// Concrete data models now live in dedicated files within `Sources/Models/`.
/// Add shared conveniences or cross-model helpers here when needed.
struct Models {}
